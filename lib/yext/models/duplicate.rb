=begin
#Yext API

#No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

The version of the OpenAPI document: 2.0

Generated by: https://openapi-generator.tech
OpenAPI Generator version: 5.4.0

=end

require 'date'
require 'time'

module Yext
  class Duplicate
    # ID of this duplicate listing
    attr_accessor :id

    # ID of the publisher site where the duplicate listing appears
    attr_accessor :publisher_id

    # ID of the location the duplicate listing is for
    attr_accessor :location_id

    # URL of the duplicate listing
    attr_accessor :url

    # The business name that appears on the duplicate listing
    attr_accessor :name

    # The address that appears on the duplicate listing
    attr_accessor :address

    # The phone number that appears on the duplicate listing
    attr_accessor :phone

    # The latitude of the location, as shown on the duplicate listing (e.g., in a map view)
    attr_accessor :latitude

    # The longitude of the location, as shown on the duplicate listing (e.g., in a map view)
    attr_accessor :longitude

    # The status of the duplicate. Note: the `DELETED` status is only available in webhook responses.
    attr_accessor :status

    # The publisher's suppression type
    attr_accessor :suppression_type

    # List of reasons why suppression is unavailable for this duplicate listing (will be empty unless **`status`** is `UNAVAILABLE`)
    attr_accessor :unavailable_reasons

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'id' => :'id',
        :'publisher_id' => :'publisherId',
        :'location_id' => :'locationId',
        :'url' => :'url',
        :'name' => :'name',
        :'address' => :'address',
        :'phone' => :'phone',
        :'latitude' => :'latitude',
        :'longitude' => :'longitude',
        :'status' => :'status',
        :'suppression_type' => :'suppressionType',
        :'unavailable_reasons' => :'unavailableReasons'
      }
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'id' => :'String',
        :'publisher_id' => :'String',
        :'location_id' => :'String',
        :'url' => :'String',
        :'name' => :'String',
        :'address' => :'String',
        :'phone' => :'String',
        :'latitude' => :'String',
        :'longitude' => :'String',
        :'status' => :'String',
        :'suppression_type' => :'String',
        :'unavailable_reasons' => :'Array<DuplicateUnavailableReason>'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `Yext::Duplicate` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!self.class.attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `Yext::Duplicate`. Please check the name to make sure it's valid. List of attributes: " + self.class.attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'id')
        self.id = attributes[:'id']
      end

      if attributes.key?(:'publisher_id')
        self.publisher_id = attributes[:'publisher_id']
      end

      if attributes.key?(:'location_id')
        self.location_id = attributes[:'location_id']
      end

      if attributes.key?(:'url')
        self.url = attributes[:'url']
      end

      if attributes.key?(:'name')
        self.name = attributes[:'name']
      end

      if attributes.key?(:'address')
        self.address = attributes[:'address']
      end

      if attributes.key?(:'phone')
        self.phone = attributes[:'phone']
      end

      if attributes.key?(:'latitude')
        self.latitude = attributes[:'latitude']
      end

      if attributes.key?(:'longitude')
        self.longitude = attributes[:'longitude']
      end

      if attributes.key?(:'status')
        self.status = attributes[:'status']
      end

      if attributes.key?(:'suppression_type')
        self.suppression_type = attributes[:'suppression_type']
      end

      if attributes.key?(:'unavailable_reasons')
        if (value = attributes[:'unavailable_reasons']).is_a?(Array)
          self.unavailable_reasons = value
        end
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      status_validator = EnumAttributeValidator.new('String', ["POSSIBLE_DUPLICATE", "SUPPRESSION_REQUESTED", "SUPPRESSED", "UNAVAILABLE", "DELETED"])
      return false unless status_validator.valid?(@status)
      suppression_type_validator = EnumAttributeValidator.new('String', ["REDIRECT", "ERROR", "MERGE", "REMOVE_FROM_SEARCH", "MOBILE", "SEARCH_PAGE"])
      return false unless suppression_type_validator.valid?(@suppression_type)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidator.new('String', ["POSSIBLE_DUPLICATE", "SUPPRESSION_REQUESTED", "SUPPRESSED", "UNAVAILABLE", "DELETED"])
      unless validator.valid?(status)
        fail ArgumentError, "invalid value for \"status\", must be one of #{validator.allowable_values}."
      end
      @status = status
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] suppression_type Object to be assigned
    def suppression_type=(suppression_type)
      validator = EnumAttributeValidator.new('String', ["REDIRECT", "ERROR", "MERGE", "REMOVE_FROM_SEARCH", "MOBILE", "SEARCH_PAGE"])
      unless validator.valid?(suppression_type)
        fail ArgumentError, "invalid value for \"suppression_type\", must be one of #{validator.allowable_values}."
      end
      @suppression_type = suppression_type
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          id == o.id &&
          publisher_id == o.publisher_id &&
          location_id == o.location_id &&
          url == o.url &&
          name == o.name &&
          address == o.address &&
          phone == o.phone &&
          latitude == o.latitude &&
          longitude == o.longitude &&
          status == o.status &&
          suppression_type == o.suppression_type &&
          unavailable_reasons == o.unavailable_reasons
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, publisher_id, location_id, url, name, address, phone, latitude, longitude, status, suppression_type, unavailable_reasons].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if attributes[self.class.attribute_map[key]].nil? && self.class.openapi_nullable.include?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = Yext.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
